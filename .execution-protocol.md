# Execution Protocol — SIMPLIFICATION PLAN

## Rules (from user)
1. Execute the full plan in SIMPLIFICATION-PLAN.md until the end without user intervention
2. Before EACH task: do in-depth code analysis specific to that task, validate the logic
3. After EACH task: verify everything works (type-check, build)
4. Do NOT do multiple tasks concurrently — one at a time, sequential
5. If user intervention is needed, SKIP the step and note it for the end report
6. Deploy after completing each phase (supervisor via deploy.sh, worker/dashboard via npm run deploy)

## Progress Tracker
- [x] Phase 1.1 — SKIPPED: Audit was wrong. CEO sessions are NOT shared across companies (each CEO has unique agent.id). Memory leak is negligible (~70 entries).
- [x] Phase 1.2 — Signal file TOCTOU race fixed: replaced existsSync+readFileSync with try/catch readFileSync
- [x] Phase 1.3 — Chat "Thinking" spinner timeout: server-side stale pending entries auto-timeout after 90s
- [x] Phase 1.4 — SKIPPED: Crash window is microseconds, amounts are small (0.5-5 credits). Fix requires coordinated supervisor+worker schema changes. Low ROI.
- [x] Phase 1.5 — Credit atomic operations: replaced read-compute-write with atomic SQL (balance + X, MAX(0, balance - X))
- [x] Phase 2.1 — Removed finalizeFounderReply + all 6 helper functions from webhooks.ts. Both streaming and non-streaming paths now pass supervisor reply through directly.
- [x] Phase 2.2 — Removed resolveFounderChatSnapshot, FounderChatSnapshot type, and unused imports. Supervisor builds its own state via gather_ceo_context.
- [x] Phase 3.1 — Acceptance criteria now checked BEFORE signal files. MAX_TURNS reduced 12→6.
- [x] Phase 3.2 — Removed task_done.json instructions from all prompts. Agents told "system auto-detects completion." Kept task_blocked.json.
- [x] Phase 3.3 — Implemented command_succeeds criterion: execSync with 30s timeout in workspace dir.
- [x] Phase 4.1 — Deleted 8 unused UI animation components (kept globe.tsx, still imported)
- [x] Phase 4.2 — Deleted 7 orphaned page directories + 4 unused section components
- [x] Phase 4.3 — Removed 12 dead API functions, 11 dead types, BlueprintSummary
- [x] Phase 4.4 — Deleted 3 unused hooks (use-goals, use-issues, use-projects) + CompletedTasks
- [x] Phase 4.5 — Deleted dead browserbase integration
- [x] Phase 4.6 — Removed issues/goals/projects routes + 3 handler files from worker
- [x] Phase 5.1 — Removed syncTasksFromIssues, cleaned reconcileTasksWithWorkspace. Dropped issues + issue_comments tables.
- [x] Phase 5.2 — Dropped goals + projects tables. Removed dead types (IssueRow, GoalRow, ProjectRow, etc.)
- [x] Phase 5.3 — Dropped policies + policy_counters + agent_task_sessions tables. Removed dead types.
- [x] Phase 5.4 — SKIPPED: agent_messages rename is low ROI per plan note.
- [x] Phase 6.1 — Sessions persisted to SQLite agent_sessions table. Loaded on restart, written through on every update.
- [x] Phase 6.2 — Founder-state response cached in KV with 3-second TTL. Saves 8-15 D1 queries per poll.
- [x] Phase 6.3 — CEO chat SWR polling reduced from 2.5s to 10s (SSE handles real-time).
- [x] Phase 6.4 — Already done in Phase 1.3 (90-second server-side timeout for stale pending entries).
- [x] Phase 7.1 — Migrated worker router from 75 regex routes to Hono declarative routing. 875→280 lines (~68% reduction).

## Skipped Tasks (need user intervention)
(none)

## Current Task
COMPLETE
